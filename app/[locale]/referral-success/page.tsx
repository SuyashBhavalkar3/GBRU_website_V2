import React from 'react';
import ReferralSuccessPage from '@/app/components/referral-success/ReferralSuccessView';

export const metadata = {
  title: 'Referral Sent Successfully - GBRU Brand Ambassador',
  description: 'Your GBRU brand ambassador referral invitation has been sent successfully.',
};

export default function ReferralSuccessRoute() {
  return <ReferralSuccessPage />;
}
