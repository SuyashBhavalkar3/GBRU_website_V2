import Login from '@/components/Login';
import React from 'react';

export const metadata = {
  title: 'Sign Up - GBRU',
  description: 'Create your GBRU account',
};

export default function SignupPage() {
  return (
    <main style={{ zoom: "0.909" }}>
      <Login />
    </main>
  );
}
