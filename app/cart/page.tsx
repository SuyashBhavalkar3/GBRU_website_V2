import React from 'react';
import Cart from '@/components/Cart';

export const metadata = {
  title: 'Your Cart - GBRU',
  description: 'Review your items and proceed to checkout.',
};

export default function CartPage() {
  return <Cart />;
}
